# P0 G4: Toolchain and board command verification

Observed 2026-09-22, Asia/Dubai. Scope: P0 tasks 0.1 G4 and 0.2. Source verification only; no installed board/tool versions, SSH connection, compilation, upload, or serial observation is claimed. Hardware-checked status for every row is PENDING. Coordinator owns environment inventory and shared ledgers.

## Published version candidates and provenance

| Component | Observed version / provenance | Status |
|---|---|---|
| Arduino UNO Q core | `arduino:zephyr@1.0.0`, Git commit `79b3f1afdad455f55e4a25030953617152c0227c`; [official package index](https://downloads.arduino.cc/packages/package_index.json) archive `https://downloads.arduino.cc/cores/zephyr/ArduinoCore-zephyr_unoq-79b3f1af.tar.bz2`, SHA-256 `d4510d5772f12d4b11df7d1b41d24f71eb3629fbd3037b127fee5ac41d673a3d` | Metadata verified; archive not downloaded or installed by this worker. |
| Core tool dependencies | Same package index selects the 13 tools listed below. | Record the full installed resolution before board work. |
| Arduino_RouterBridge | `0.4.3`, SHA `d681b87211864148367f2d77096ae84c5badcb2f`; [library.properties](https://github.com/arduino-libraries/Arduino_RouterBridge/blob/0.4.3/library.properties) depends on Arduino_RPClite without an exact version. | Candidate pin; safety conflict in G3. |
| Arduino_RPClite | `0.3.1`, SHA `871a213aefa53020f5a6881a3a99d0f66d172fd7`; [library.properties](https://github.com/arduino-libraries/Arduino_RPClite/blob/0.3.1/library.properties) requires MsgPack >=0.4.2. | Candidate pin; exact MsgPack dependency resolution remains UNKNOWN. Inspect library index and installed list before final locking. |
| ArduinoGraphics | `1.1.5`, SHA `cd50e84e95c5a5ec645c8adda52af2fcb7cc4ab8`; [metadata](https://github.com/arduino-libraries/ArduinoGraphics/blob/1.1.5/library.properties). Core Arduino_LED_Matrix metadata is `0.1.3` and declares ArduinoGraphics. | Source pin only; installed status UNKNOWN. |
| Router / app CLI | Official release API returned arduino-router `v0.10.0` and arduino-app-cli `v0.13.0` (both 2026-08-11). | Published status only; do not assume board image includes these releases. |

The official GitHub release endpoint for remoteocd returned 0.1.0 as latest, while the board package index selects **0.1.1** and its tag resolves to `7109bed7d4f138795d7a5c07c0b9d292fd81d77d`. Use package-resolved versions, not a release-page heuristic. Sources: [remoteocd 0.1.1](https://github.com/arduino/remoteocd/tree/0.1.1), [release API](https://api.github.com/repos/arduino/remoteocd/releases/latest).

Recovery review, 2026-09-22: the original six-tool list was incomplete. Full
core 1.0.0 package-index dependencies (not an installed inventory):
`arduino/adb@32.0.0`, `zephyr/arm-zephyr-eabi@1.0.1`,
`arduino/bin2uf2@0.1.0`, `arduino/bossac@1.9.1-arduino2`,
`arduino/dfu-util@0.11.0-arduino5`, `arduino/gen-rodata-ld@0.1.1`,
`SiliconLabs/openocd@0.12.0-arduino1-static`, `arduino/remoteocd@0.1.1`,
`arduino/rp2040tools@1.1.0-rc1`, `arduino/scripting-tools@0.0.7`,
`arduino/sync-zephyr-artifacts@0.2.2`, `arduino/zephyr-check-size@0.1.0`,
`arduino/zephyr-sketch-tool@0.4.1`. The link recipe directly invokes
`gen-rodata-ld`; the preflight must not assume the omitted tools are optional.
Source: official package index above and core 1.0.0 platform.txt.

## Commands and findings

| Question | Verified answer / uncertainty | Primary evidence / check |
|---|---|---|
| Target and options | FQBN `arduino:zephyr:unoq`; Immediate suffix `:wait_linux_boot=no`. | [core 1.0.0 boards.txt](https://github.com/arduino/ArduinoCore-zephyr/blob/1.0.0/boards.txt); installed `board details` still required. |
| Compile on board | Official remoteocd guidance uses `arduino-cli compile -b arduino:zephyr:unoq <sketch-dir>`. Add a controlled `--build-path` or `--output-dir` and preserve that artifact for upload. `compile` without `--upload` is build-only. | [remoteocd README](https://github.com/arduino/remoteocd/blob/0.1.1/README.md); [CLI compile reference](https://docs.arduino.cc/arduino-cli/commands-reference/arduino-cli_compile/). Verify installed CLI help and inspect compile hooks before relying on board-specific behavior. |
| Upload on board | Official guidance uses `arduino-cli upload -b arduino:zephyr:unoq <sketch-dir>`; its remoteocd detects the on-board environment and uses local OpenOCD. This is a separate reset/flash/run action, never implied by compiling. | [remoteocd main.go](https://github.com/arduino/remoteocd/blob/0.1.1/main.go); [platform upload recipes](https://github.com/arduino/ArduinoCore-zephyr/blob/1.0.0/platform.txt). Motor-capable upload remains per-run human-gated. |
| `/dev/ttyHS1` upload port seed | CONFLICT: this is the Linux serial device claimed by arduino-router, not a verified upload-port argument. The official manual reserves it from direct application use. Do not pass it as a guessed flash/monitor port. | [UNO Q user manual, Router resources](https://docs.arduino.cc/tutorials/uno-q/user-manual/); versioned remoteocd/core sources above. |
| SSH route | Stage locally, copy to an explicit board directory, execute Arduino CLI on the board through SSH with host-key checking. This preserves D-009's on-board build choice; compile-only must end after compilation. | [remoteocd README modes](https://github.com/arduino/remoteocd/blob/0.1.1/README.md). SSH alias/user, credentials availability, rsync, remote paths and CLI installation remain UNKNOWN; coordinator obtains missing configuration. |
| adb fallback | Source verifies core upload integration with adb/USB serial ID; remoteocd accepts `--serial` and `--adb-path`. For the required on-board build workflow, `adb -s <verified-id> push` and `adb -s <verified-id> shell` are transfer/command equivalents to verify first. Never select the first attached device silently. | [remoteocd main.go](https://github.com/arduino/remoteocd/blob/0.1.1/main.go); [ADB implementation](https://github.com/arduino/remoteocd/blob/0.1.1/board/adb.go); [core platform](https://github.com/arduino/ArduinoCore-zephyr/blob/1.0.0/platform.txt). No attached device, shell user, host driver, board image permission or serial ID verified. |
| Install libraries on board | CLI syntax supports `arduino-cli lib install 'Name@version'`; record `lib list` and all transitive versions, with `core list`, after installation. Proposed explicit candidates above are not evidence of a successful installation. | [CLI lib install](https://docs.arduino.cc/arduino-cli/commands-reference/arduino-cli_lib_install/). Check installed CLI help/config before changing board packages; avoid silent firmware/core upgrades. |
| Monitor CLI | `arduino-app-cli monitor` is an official command. It opens the local router monitor at `127.0.0.1:7500` and is bidirectional. Its stdin-reader closes the connection on EOF, so `</dev/null` is not a reliable receive-only invocation. | [command](https://github.com/arduino/arduino-app-cli/blob/v0.13.0/cmd/arduino-app-cli/monitor/monitor.go); [implementation](https://github.com/arduino/arduino-app-cli/blob/v0.13.0/internal/monitor/monitor.go). |
| Read-only project logging | A Python TCP client executing on the board through SSH can connect to `127.0.0.1:7500` and only `recv`, forwarding bytes to stdout without sending monitor input. This is an implementation proposal derived from the router listener, not a completed board test. | [router listener and methods](https://github.com/arduino/arduino-router/blob/v0.10.0/internal/monitorapi/monitor-api.go); [router default address](https://github.com/arduino/arduino-router/blob/v0.10.0/main.go). Validate installed service address first; do not use journalctl router service diagnostics as if they were MCU Monitor payload. |

## Remaining acceptance checks

1. Obtain board SSH alias/address and user, confirmed bare-board/motor-isolated condition, and exact image/tool versions. Never collect or print passwords/private keys.
2. Read-only remote preflight: `uname -a`, CLI version/core list/board details/lib list, existence of configured directories and required commands, router version/service listener. Save outputs with credentials excluded.
3. Verify staging: `.ino` at `build/stage/<name>/<name>.ino`, config/core/HAL beneath its `src/`, no sibling-source assumptions. Compile actual staged source against installed dependencies; desktop substitutes are script tests only.
4. Run controlled-command script tests covering invalid arguments, missing config, absent tools, failed compile, source paths with spaces, and every `--match`/`--compile-only` combination. Assert compile-only never calls upload/reset/start and never masks nonzero return codes.
5. Perform inert board compilation/upload only when physical isolation is known. P0 matrix+Monitor demonstration additionally awaits the R3/R4 protected decision in G3. Record compile, upload and observed output as separate results.
6. adb is fallback-pending until an explicitly identified device and harmless shell/transfer checks succeed. If it is unavailable, fail explicitly; do not report SSH script substitutes as adb validation.

Every UNKNOWN above has its corresponding preflight, source-resolution, or physical round-trip check here. No GATE P0 PASS, PINMAP OK, STAND OK, or RING OK is asserted.
