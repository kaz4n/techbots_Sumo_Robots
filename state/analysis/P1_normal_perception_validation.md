# Normal perception validation, 2026-09-22 Asia/Dubai

HOST-TESTED: b17696d contract then aae9b36 implementation and16 independent
spec/header-derived cases. Front priority and three centered observations select
TRACK/ATTACK; front loss brakes immediately and selects the current residual
destination. Script/preemption qualification reset remains the caller contract.
Source and established locked tests/config unchanged outside the reviewed scope.

Actual commands and receipts (all exit0):
- wsl -d Ubuntu -- bash tools/test_host.sh: P1_normal_perception_build.txt; CTest1/1.
- wsl -d Ubuntu -- cmake --build build/host-sanitize --parallel 2:
  P1_normal_perception_sanitizer_build.txt.
- wsl -d Ubuntu -- build/host-sanitize/sumox26_tests:
  P1_normal_perception_sanitizer.txt;625 cases/11,994,540 assertions, zero failed
  or skipped, no ASan/UBSan diagnostics.

Separate reused read-only review independently reproduced script/binary results,
reported no findings, and reviewed exact23-file inert hashes before refresh.
See ../reviews/P1_normal_perception_codex.md. No failed runtime fix cycle.
No full Robot, target build, MotorGate boundary or physical evidence is claimed.

During completion the user connected a bare UNO Q and authorized inert testing.
That new P0 work is recorded separately; it does not close a human phase gate.
C: had236,900,352 bytes free before these final runs; no new tool download needed.

After independently approved manifest refresh, command
`wsl -d Ubuntu -- python3 -m unittest discover -s tests/tooling -v` passed48/48,
exit0; receipt P1_normal_perception_tools.txt. These are controlled script tests,
not board evidence. No existing locked test changed.
