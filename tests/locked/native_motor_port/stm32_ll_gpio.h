// Exposes the installed common LL GPIO selector for the U5 fixture.
// It introduces no extra API or behavior beyond the source-verified U5 reads.
// Native contract cases use either installed include form.
#pragma once
#include "stm32u5xx_ll_gpio.h"
